get_tenants:
  handler: services/tenant_manager/get_tenants/lambda_function.handler
  events:
    - http:
        method: GET
        path: /tenant_managment/get_tenants
  package:
    patterns: 
      - services/tenant_manager/get_tenants/lambda_function.py
  environment:
    TABLE_NAME: ${self:custom.tenantTable}
  iamRoleStatements:
    - Effect: "Allow"
      Action: 
        - dynamodb:Scan
      Resource: 
        Fn::GetAtt: [TenantTableDynamo, Arn]