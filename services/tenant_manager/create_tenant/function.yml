create_tenant:
  handler: services/tenant_manager/create_tenant/lambda_function.handler
  events:
    - http:
        method: POST
        path: /tenant_managment/create_tenant
        private: true
  package:
    patterns: 
      - services/tenant_manager/create_tenant/lambda_function.py
  environment:
    TABLE_NAME: ${self:custom.tenantTable}
  iamRoleStatements:
    - Effect: "Allow"
      Action: 
        - dynamodb:PutItem
      Resource: 
        Fn::GetAtt: [TenantTableDynamo, Arn]