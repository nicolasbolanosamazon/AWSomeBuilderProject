tenant_onboarding:
  handler: services/tenant_onboarding/lambda_function.handler
  timeout: 15
  events:
    - http:
        method: POST
        path: /onboard_tenant
  package:
    patterns:
      - services/tenant_onboarding/lambda_function.py
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - secretsmanager:GetSecretValue
      Resource: 'arn:aws:secretsmanager:us-east-1:238181680470:secret:dev-api-keys-TTv22n'